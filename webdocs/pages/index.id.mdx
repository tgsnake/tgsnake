--- 
title : Linggardjati 
---
import Callout from 'nextra-theme-docs/callout'
import {TabGroup,TabContent} from "@/components/Tab"

![tgsnake-icon-by-flat-icon](./images/tgsnake.jpg)
  
<TabGroup>
    <TabContent name="npm">
        ```bash 
        npm install tgsnake@latest -s
        ```
    </TabContent>
    <TabContent name="yarn">
        ```bash 
        yarn add tgsnake@latest 
        ```
    </TabContent>
</TabGroup>
setelah menginstal paket. 
<TabGroup>
    <TabContent name="javascript"> 
    ```js
    // index.js
    const {Snake} = require("tgsnake") // mengimpor modul.
    const bot = new Snake({ // membuat klien baru. Anda dapat menaruh opsi di file tgsnake.config.js.
      apiHash : "tempel disini", // dapatkan dari my.telegram.org
      apiId : 1234, // dapatkan dari my.telegram.org
      botToken : "tempel disini" // jika anda ingin login dengan akun anda, hapus ini dan buat sesi terlebih dahulu. lalu tempel sesi anda di bidang "session".
    }) 
    bot.run() // jalankan client. 
    // bot.generateSession() // aktifkan ini untuk menghasilkan sesi dan nonaktifkan bot.run().
    bot.hears("hai",(ctx)=>{
        ctx.reply("wow kamu berhasil.")
    })
    ```
    </TabContent> 
    <TabContent name="typescript"> 
    ```ts
    // index.ts
    import {Snake} from "tgsnake" // mengimpor mo
    const bot = new Snake({ // membuat klien baru. Anda dapat menaruh opsi di file tgsnake.config.js.
      apiHash : "tempel disini", // dapatkan dari my.telegram.org
      apiId : 1234, // dapatkan dari my.telegram.org
      botToken : "tempel disini" // jika anda ingin login dengan akun anda, hapus ini dan buat sesi terlebih dahulu. lalu tempel sesi anda di bidang "session".
    }) 
    bot.run() // jalankan client. 
    // bot.generateSession() // aktifkan ini untuk menghasilkan sesi dan nonaktifkan bot.run().
    bot.hears("hai",(ctx)=>{
        ctx.reply("wow kamu berhasil.")
    })
    ```
    </TabContent>
</TabGroup>

<Callout type="warning" >
Akun telegram Anda bisa dibanned jika Anda membuat userbot dan membuat flooding. Saya tidak bertanggung jawab jika hal ini terjadi.
</Callout>

<div className="text-center py-2">
[Telegram Channel](https://t.me/tgsnake) - 
[Telegram Chat](https://t.me/tgsnakechat) 
</div>