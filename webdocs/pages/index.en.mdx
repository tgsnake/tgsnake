---
title : Linggadjati
---
import Callout from 'nextra-theme-docs/callout'
import {TabGroup,TabContent} from "@/components/Tab"

![tgsnake-icon-by-flat-icon](./images/tgsnake.jpg)

<TabGroup>
    <TabContent name="npm">
        ```bash 
        npm install tgsnake@latest -s
        ``` 
    </TabContent>
    <TabContent name="yarn">
        ```bash 
        yarn add tgsnake@latest 
        ``` 
    </TabContent>
</TabGroup>
setelah menginstal paket.
<TabGroup>
    <TabContent name="javascript">
    ```js
    // index.js
    const {Snake} = require("tgsnake") //mengimport module.
    const bot = new Snake({ // membuat client baru. kamu bisa mengisi opsinya di file tgsnake.config.js.
      apiHash : "paste disini", // dapatkan dari my.telegram.org
      apiId : 1234, // dapatkan dari my.telegram.org
      botToken : "paste disini" // jika kamu butuh login dengan user, hapus ini dan buat session dulu. kemudian paste sesi string kamu di tempat "session".
    }) 
    bot.run() // menjalankan klien. 
    // bot.generateSession() // aktifkan ini untuk menghasilkan sesi dan mematikan bot.run().
    bot.hears("hi",(ctx)=>{
        ctx.reply("wow you are perfect")
    })
    ```
    </TabContent>
    <TabContent name="typescript">
    ```ts
    // index.ts
    import {Snake} from "tgsnake" //mengimport modul.
    const bot = new Snake({ // membuat klien baru. kamu bisa mengisi opsinya di file tgsnake.config.js.
      apiHash : "paste disini", // dapatkan dari my.telegram.org
      apiId : 1234, // dapatkan dari my.telegram.org
      botToken : "paste disini" // jika kamu butuh login dengan user, hapus ini dan buat session dulu. kemudian paste sesi string kamu di tempat "session".
    }) 
    bot.run() // menjalankan klien. 
    // bot.generateSession() // aktifkan ini untuk menghasilkan sesi dan mematikan bot.run().
    bot.hears("hi",(ctx)=>{
        ctx.reply("wow you are perfect")
    })
    ```
    </TabContent>
</TabGroup>

<Callout type="warning">
Akun telegram kamu bisa ter-banned jika kamu membuat userbot dan membuat flooding. Saya tidak bertanggung jawab jika ini terjadi.
</Callout>

<div className="text-center py-2">
[Channel Telegram](https://t.me/tgsnake) - 
[Chat Telegram](https://t.me/tgsnakechat) 
</div>